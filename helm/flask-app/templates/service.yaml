apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}

  selector:
    app: flask-app

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.containerPort }}
